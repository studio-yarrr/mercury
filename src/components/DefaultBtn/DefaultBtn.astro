---
interface Props {
  as?: astroHTML.JSX.IntrinsicElements;
  viewType?: "width" | "icon";
  theme?: "stroke";
  size?: "sm";
  color?: "inverse" | "secondary" | "secondary-inverse";
  className?: string;
}

const {
  as: Wrapper = "button",
  viewType,
  size,
  color,
  theme,
  className,
  ...rest
} = Astro.props;
---

<Wrapper
  {...rest}
  class:list={["default-btn", viewType, size, color, theme, className]}
>
  <slot />
</Wrapper>
